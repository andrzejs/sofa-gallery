%ul.gallery
  - @gallery.photos.each do |photo|
    %li{:id => dom_id(photo)}
      .photo
        = link_to image_tag(photo.image.url(:thumb), :alt => ''), photo.image.url(:medium), :rel => "prettyPhoto[#{dom_id(@gallery)}]", :title => "photo.description"